{
  "name": "@ai-marketplace/core",
  "version": "1.0.1",
  "private": true,
  "license": "GPL-3.0-only",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "compile:test": "tsc -p tsconfig.test.json",
    "lint": "eslint src test --ext ts",
    "test": "npm run build && npm run compile:test && node --test \"out-test/test/*.test.js\""
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": { "@types/node": "^26.4.1", "typescript": "^6.0.3" }
}
